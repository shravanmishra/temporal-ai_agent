[project]
name = "temporal-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=3.16.2",
    "psutil>=7.2.2",
    "python-dotenv>=1.2.3",
    "temporalio>=1.33.0",
]

[project.scripts]
temporal-agent = "temporal_agent:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
